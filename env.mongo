# MongoDB
MONGODB_URI=mongodb://docbao_user:docbao_password@localhost:27017/docbao_db?authSource=admin

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Server
PORT=3000
NODE_ENV=development

# OTP (sẽ sử dụng sau)
OTP_SECRET=your-otp-secret-key
OTP_EXPIRES_IN=300 